<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.adesso.wieschalla.senses2go">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GPSActivity" />
        <activity android:name=".GyroActivity" />
        <activity android:name=".MagnoActivity" />
        <activity android:name=".AccActivity" />
        <activity android:name=".ProxActivity" />
        <activity android:name=".AirActivity1" />
        <activity android:name=".AirActivity2" />
        <activity android:name=".LightActivity" />
        <activity android:name=".CameraActivity" />
        <activity android:name=".SoundActivity" />
        <activity android:name=".MicActivity"></activity>
    </application>

</manifest>